============
Installation
============

At the command line::

    $ easy_install pharmcrm2-stocks

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pharmcrm2-stocks
    $ pip install pharmcrm2-stocks
